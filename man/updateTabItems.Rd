% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widgets-sidebar.R
\name{updateTabItems}
\alias{updateTabItems}
\title{Update the selected sidebar tab item}
\usage{
updateTabItems(
  session = shiny::getDefaultReactiveDomain(),
  inputId,
  selected = NULL
)
}
\arguments{
\item{session}{Shiny session.}

\item{inputId}{The shared \code{id} used for \code{sidebarMenu(id)}, \code{tabItems(id)},
and \code{updateTabItems(inputId)}. All three must use the same value.}

\item{selected}{Name of the tab to select.}
}
\description{
This updates the hidden body tabset used by \code{\link[=tabItems]{tabItems()}} and keeps the
sidebar active item synchronized with the selected tab.
}
\examples{
if (interactive()) {
shiny::shinyApp(
  ui = dashboardPage(
    header = dashboardHeader(title = "Demo"),
    sidebar = dashboardSidebar(
      sidebarMenu(
        id = "sidebarMenu",
        menuItem("Overview", tabName = "overview"),
        menuItem("Reports", tabName = "reports")
      )
    ),
    body = dashboardBody(
      shiny::actionButton("go_reports", "Go to reports"),
      tabItems(
        tabItem(tabName = "overview", shiny::h2("Overview")),
        tabItem(tabName = "reports", shiny::h2("Reports"))
      )
    )
  ),
  server = function(input, output, session) {
    shiny::observeEvent(input$go_reports, {
      updateTabItems(session, inputId = "sidebarMenu", selected = "reports")
    })
  }
)
}

}
