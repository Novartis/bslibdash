% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{dashboardPage}
\alias{dashboardPage}
\title{Branded dashboard page}
\usage{
dashboardPage(
  header,
  sidebar,
  body,
  title = NULL,
  footer = NULL,
  theme = brand_bs_theme()
)
}
\arguments{
\item{header}{Slot for \code{\link[=dashboardHeader]{dashboardHeader()}}.}

\item{sidebar}{Slot for \code{\link[=dashboardSidebar]{dashboardSidebar()}}.}

\item{body}{Slot for \code{\link[=dashboardBody]{dashboardBody()}}.}

\item{title}{Page/app title shown in the browser tab and dashboard header.}

\item{footer}{Optional slot for \code{\link[=dashboardFooter]{dashboardFooter()}}.}

\item{theme}{A \code{bslib} theme. Defaults to \code{\link[=brand_bs_theme]{brand_bs_theme()}}.}
}
\value{
A Shiny UI definition.
}
\description{
A bslib sidebar layout that arranges the four standard dashboard slots.
The \code{title} argument, when set, replaces the title text inside \code{header}
if \code{dashboardHeader()} was called without one.
}
\examples{
ui <- dashboardPage(
  header = dashboardHeader(title = "bslibdash dashboard"),
  sidebar = dashboardSidebar(
    sidebarMenu(
      id = "sidebarMenu",
      menuItem("Overview", tabName = "overview")
    )
  ),
  body = dashboardBody(
    tabItems(
      tabItem(
        tabName = "overview",
        box("Overview content", title = "Overview")
      )
    )
  )
)
}
