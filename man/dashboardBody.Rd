% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/body.R
\name{dashboardBody}
\alias{dashboardBody}
\alias{tabItems}
\alias{tabItem}
\title{Dashboard body}
\usage{
dashboardBody(...)

tabItems(..., id = "sidebarMenu")

tabItem(tabName = NULL, ...)
}
\arguments{
\item{...}{Items to put in the container.}

\item{id}{Shared Shiny id for the sidebar menu and body tabset. Use the same
value in \code{sidebarMenu(id)}, \code{tabItems(id)}, and
\code{updateTabItems(inputId)}. Defaults to \code{"sidebarMenu"}.}

\item{tabName}{The name of a tab.}
}
\value{
\code{dashboardBody()} returns a \code{shiny.tag} \verb{<div>} wrapping the main
content area of the dashboard.

\code{tabItems()} returns a \code{shiny.tag} \verb{<div>} containing a hidden
\code{\link[shiny:tabsetPanel]{shiny::tabsetPanel()}} whose panels are switched by the sidebar menu.

\code{tabItem()} returns a \code{shiny.tag.list} holding the tab content, with
the tab name recorded in its \code{tabName} attribute so that \code{tabItems()} can
place it in the matching panel.
}
\description{
Containers for tab content placed inside \code{\link[=dashboardBody]{dashboardBody()}}:
}
\details{
\itemize{
\item \code{tabItems()} is the parent container.
\item \code{tabItem(tabName)} is one tab; \code{tabName} must match the
corresponding \code{menuItem()} \code{tabName}.
}
}
\examples{
dashboardBody(
  tabItems(
    tabItem(tabName = "overview", shiny::h2("Overview")),
    tabItem(tabName = "reports", shiny::h2("Reports"))
  )
)
tabItems(
  tabItem(tabName = "overview", shiny::p("Overview content")),
  tabItem(tabName = "reports", shiny::p("Reports content"))
)
tabItem(
  tabName = "overview",
  shiny::h2("Overview"),
  shiny::p("This is the overview tab.")
)
}
