% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cards.R, R/aliases.R
\name{box}
\alias{box}
\alias{updateBox}
\alias{updateCard}
\title{Create a card}
\usage{
box(
  ...,
  title = NULL,
  footer = NULL,
  status = NULL,
  background = NULL,
  width = 6,
  height = NULL,
  collapsible = FALSE,
  collapsed = FALSE,
  closable = FALSE,
  maximizable = FALSE,
  icon = NULL,
  label = NULL,
  id = NULL
)

updateBox(
  id,
  action = c("remove", "toggle", "update", "restore", "toggleMaximize"),
  options = NULL,
  session = shiny::getDefaultReactiveDomain()
)

updateCard(
  id,
  action = c("remove", "toggle", "update", "restore", "toggleMaximize"),
  options = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{...}{Contents of the card.}

\item{title}{Optional title.}

\item{footer}{Optional footer.}

\item{status}{Bootstrap status color applied to the \strong{card header} background.
Accepted values are Bootstrap semantic names: \code{"primary"}, \code{"secondary"},
\code{"success"}, \code{"info"}, \code{"warning"}, \code{"danger"}, \code{"light"}, \code{"dark"}.
Unlike \code{\link[=valueBox]{valueBox()}} and \code{\link[=infoBox]{infoBox()}}, legacy \code{shinydashboard} color names
(e.g. \code{"aqua"}, \code{"blue"}) are \strong{not} supported here.}

\item{background}{Bootstrap status color applied to the \strong{entire card}
background. Accepts the same values as \code{status}. When set, the full card
surface is coloured rather than just the header.}

\item{width}{Width of the card. An integer \code{1}–\code{12} is treated as Bootstrap
grid columns and the card is automatically wrapped in \code{shiny::column()},
consistent with \code{\link[=valueBox]{valueBox()}} and \code{\link[=infoBox]{infoBox()}}. Any other value (e.g.
\code{"300px"}) is applied as an inline CSS \code{width}. Use \code{NULL} when placing
the card inside an existing column or a \code{boxLayout()}.}

\item{height}{Card height.}

\item{collapsible}{Whether the card body can collapse.}

\item{collapsed}{Whether the card starts collapsed.}

\item{closable}{Whether the card can be hidden.}

\item{maximizable}{Whether the card can be displayed full-screen.}

\item{icon}{Header icon tag or icon name.}

\item{label}{Header label content.}

\item{id}{Id of the card created with \code{\link[=box]{box()}}.}

\item{action}{Action to trigger.}

\item{options}{List of new options for \code{action = "update"}.}

\item{session}{Shiny session.}
}
\description{
Create a card

Update a card from the server side

Backwards-compatible alias to \code{\link[=updateBox]{updateBox()}}
}
\examples{
box(
  "Card body",
  title = "Status",
  status = "primary",
  id = "status_box"
)

boxLayout(
  box("A", title = "Card A"),
  box("B", title = "Card B"),
  type = "deck"
)

if (interactive()) {
shiny::shinyApp(
  ui = bslib::page_fluid(
    shiny::actionButton("toggle", "Toggle card"),
    box("Card body", title = "Status", id = "status_box")
  ),
  server = function(input, output, session) {
    shiny::observeEvent(input$toggle, {
      updateBox("status_box", action = "toggle", session = session)
    })
  }
)
}

}
\seealso{
Other cards:
\code{\link[=boxLayout]{boxLayout()}}
}
\concept{cards}
