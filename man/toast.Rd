% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedbacks.R
\name{toast}
\alias{toast}
\title{Create a toast}
\usage{
toast(
  title,
  body = NULL,
  subtitle = NULL,
  options = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{title}{Toast title.}

\item{body}{Body content.}

\item{subtitle}{Toast subtitle. Rendered as an additional muted body block
below \code{body} (footnote).}

\item{options}{Toast options. Supports \code{delay} (auto-hide duration in
milliseconds), \code{autohide} (set \code{FALSE} to disable auto-hide), \code{icon}
(optional header icon tag), \code{close} (show the close button, default
\code{TRUE}), and \code{type} (Shiny notification status: one of \code{"default"},
\code{"message"}, \code{"warning"}, \code{"error"}).}

\item{session}{Shiny session object.}
}
\description{
Renders a notification through \code{\link[shiny:showNotification]{shiny::showNotification()}}. The default
Shiny / bslib notification chrome (background, border, position, status
color, dismiss button) is used as-is; \code{toast()} only adds support for the
extra parameters below — header (title + optional icon), body, and
subtitle footnote — emitted as flat blocks so they do not produce a
card-in-notification (box-in-box) look. The \code{type} option in \code{options}
controls the status color via Shiny's own notification statuses
(\code{"default"}, \code{"message"}, \code{"warning"}, \code{"error"}).
}
\examples{
\dontrun{
shiny::shinyApp(
  ui = bslib::page_fluid(shiny::actionButton("show_toast", "Show toast")),
  server = function(input, output, session) {
    shiny::observeEvent(input$show_toast, {
      toast(
        title = "Heads up",
        body = "Background job finished with a warning.",
        options = list(type = "warning", delay = 3000),
        session = session
      )
    })
  }
)
}

}
