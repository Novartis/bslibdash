% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_bs_theme.R
\name{brand_bs_theme}
\alias{brand_bs_theme}
\title{Default bslibdash brand theme}
\usage{
brand_bs_theme()
}
\value{
A \code{bs_theme} object (also inheriting \code{bs_version_5} and
\code{sass_bundle}) carrying the bslibdash brand colour, typography and shape
variables. Pass it to the \code{theme} argument of \code{\link[=dashboardPage]{dashboardPage()}}, or extend
it with the \verb{bslib::bs_add_*()} helpers.
}
\description{
Builds a Bootstrap 5 theme via \code{\link[bslib:bs_theme]{bslib::bs_theme()}} with the bslibdash brand
colour, typography and shape variables.
}
\details{
Component CSS is no longer bundled into the theme: each bslibdash component
attaches its own theme-aware \code{\link[bslib:bs_dependency]{bslib::bs_dependency_defer()}} dependency, so
rendering a component with any \code{bslib::bs_theme()} produces the matching
bslibdash styles. \code{brand_bs_theme()} exists to set the Bootstrap-level
variables used as defaults by bslibdash page constructors.

\code{dashboardPage()} applies this theme by default. To customise it, override
variables or rules via bslib and pass the result through the page \code{theme}
argument:

\preformatted{
dashboardPage(
  header = dashboardHeader(),
  sidebar = dashboardSidebar(),
  body = dashboardBody(),
  theme = brand_bs_theme() |>
    bslib::bs_add_variables(primary = "#8B0000")
)
}
}
\examples{
theme <- brand_bs_theme()
class(theme)
}
